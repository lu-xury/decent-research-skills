# AI 研究判断原则

## 1. 选择能够改变认识或实践的问题

先说明谁需要什么能力，以及解决后能够作出什么更可靠的决策。价值可以体现为新的任务能力、对现象的解释、更合适的表示、更低的资源成本，或更有效的评价。

操作：写一句“解决后，可以在什么条件下更好地完成什么”。列出两个现有分数无法区分的情形。例如同样平均正确率的模型，可能具有不同的失败集中度或重复可靠性；先决定这些区别是否影响实际使用，再制定指标。

验收：问题有明确对象和使用条件，目标指标能够区分真正关心的结果。

## 2. 将现象与机制分开，用不同预测裁决解释

同一现象可能来自容量、优化、数据、信息、协议或评价。为主要解释写出可观察预测，主动改变一个因素并控制其他因素，让解释产生不同结果。

操作：记录“解释—干预—预计观察—替代解释”。例如判断误差来自信息量还是呈现顺序时，保持信息内容一致再改变顺序。简化系统用于检验某个机制是否足以产生现象；实际系统实验用于判断它是否是现实中的重要原因。

验收：至少一项实验可能改变当前机制判断。观察符合某个解释时，同时说明仍有哪些解释没有排除。

## 3. 根据语义、依赖和复用机会设计表示

先确定关键变量、查询对象、局部与全局关系，以及跨查询共享的上下文。需要精确控制或容易监督的量可显式保留，其余信息可以压缩；压缩质量由目标任务而非单一重建分数评价。

操作：画输入到输出的依赖图，区分必需关系、待验证关系与可以共享的计算。如果许多独立查询使用同一上下文，可计算一次上下文再逐查询读出；若查询之间存在真正依赖，则设计对应交互并验证其必要性。

验收：输入替换、状态交换或查询重排能够检验依赖判断；记录压缩保留了什么，以及丢失了什么。

## 4. 使复杂度与瓶颈相匹配

简单模型便于验证，大模型能够提供额外容量和计算深度。两者都是可用选择。优先检查数据、目标和优化是否允许能力得到表达，再确定需要多大模型。

操作：使用相同数据或固定经验做宽度、深度和训练预算对照。在线学习中还要区分大模型改变探索后带来的数据差异。确认小基线经过合理优化后，再把缩放趋势解释为容量证据。

验收：新增规模或模块对应可测能力，收益在适当预算轴上成立；实际推理、训练与存储成本均有记录。

## 5. 将数据设计为识别关系的实验

有效数据不仅覆盖常见样本，还要提供区分目标关系的变化。已有观测、隐含行为标注、可验证结果、解析生成器和数值求解器都可提供信号，各自有适用范围和误差。

操作：为每个学习目标说明标签从哪里来、是否充分、有哪些混淆变量和捷径。设计受控变化并验证不变量；改变正确答案的扰动需要重新取得目标。按独立轨迹、场景、对象家族或母样本分组切分。

验收：能够说明数据如何识别所需能力，兄弟样本与近重复样本得到正确分组，标注与筛选成本可核算。

## 6. 通过信息流和梯度流选择训练范式

学习范式由实际可用信号决定。监督适合具有可靠目标的任务；自监督需要代理目标能够约束所需能力；生成方法处理多个合理答案；强化学习适合确有长期信用、离散决策或不可微反馈价值的场景。

操作：逐阶段画出输入、采样、预测、评分和损失，标出更新参数、冻结模块、停止梯度、移动平均教师和历史来源。区分由正确状态输入训练与由模型自身输出驱动训练，检查部署时每个条件如何获得。

验收：目标的信息来源与实际更新方向明确；课程解决已识别的能力缺口，额外训练和评估成本得到披露。

## 7. 以多维指标区分能力、可靠性与效用

单次成功率反映一次使用的可靠性；多次采样至少成功一次的比例反映给定采样和选择条件下的覆盖，二者不能互换。生成质量、多样性、长期一致性和实际可用性也应按任务分别评价。

操作：选择一个服务核心主张的主指标，再用能揭示取舍的指标补充。例如纠错同时测修复错误的比例与破坏原正确答案的比例；长期预测同时测短期误差与任务相关长期统计。使用验证器选择候选时计入验证能力和成本。

验收：平均分之外能解释主要失败模式，评价覆盖声明的部署条件。

## 8. 用独立证据检查代理目标

训练奖励、自动评分器、感知分数和数值参考都只是目标的测量通道。系统可能提高代理得分，却没有改善最终行为。

操作：选择不同标注通道、参考方法或人工抽查检验关键结论。分别检查评分器参数、输入分布和被评行为的变化。将评分器校准集与最终结论集分开，并记录评分误差和不确定性。

验收：核心收益获得与训练评分不同来源的支持；无法独立验证时，将结论限定为代理指标改善。

## 9. 在真实资源与时间尺度上比较

参数量、token、迭代次数、网络求值、环境交互和墙钟时间描述不同成本。一次算法更新可能包含多次前向、评分或搜索，一次成功预测也可能依赖大量离线生成。

操作：确定实际瓶颈，分别统计数据生成、训练、初始化、推理、查询、回退和峰值内存。保持质量要求一致，或给出质量—成本曲线。固定某一预算时写清哪些资源仍然变化，重复使用场景说明离线成本如何摊销。

验收：效率结论可由完整账本核对，参考方法得到合理实现和调参。

## 10. 让证据强度与结论范围一致

理论需要明确假设、信息访问、误差范数、随机性、量词和资源；实验需要明确场景、规模、样本相关性与不确定性；应用需要验证接口和任务效用。

操作：为每个核心结论建立“证据—条件—尚未覆盖部分”记录。将理想构造、局部测试和真实部署分别标记；检索边界、经验规律和证明结论各自使用相应表达。

验收：每项主张都有对应支持，并保留足以改变结论的重要条件。

## 综合使用

研究按“有价值的问题—可检验解释—相称的设计—能识别机制的数据—可裁决的证据”组织。创新可以位于问题、方法、训练、数据、评价、理论或系统；贡献归属由实验和分析决定。

每一阶段选择最可能减少关键不确定性的工作。探索与确认分开记录，允许根据证据简化、扩展或重构方法。正式文档聚焦当前研究对象、设计与验证，直接表达可执行的知识。
